## 1. CLI 与界面依赖解耦

- [x] 1.1 将 `presentation.py` 的 Rich 手册渲染移至仅由 TUI 导入的模块，保留标准库错误脱敏；通过隔离子进程检查 CLI／真实 Codex 执行路径不导入 Rich 或 Textual，并通过脱敏测试。
- [x] 1.2 实现标准库帮助与列表表格，复用 `catalog.py`；在 `tests/test_cli_presentation.py` 验证全部 13 项帮助、中英文、空列表、长 URL、混合宽度文字、无 ANSI 及 JSON 原字段／退出码。
- [x] 1.3 将依赖移入 `[tui]` extra，保留资源打包，增加基础与完整安装元数据测试；在独立基础 venv 验证没有第三方运行依赖，在完整 venv 验证 UI 和样式可导入。
- [x] 1.4 按设计入口矩阵实现可选依赖探测和双语提示；测试无参数、仅语言参数、显式 tui、help、非 TTY、TERM=dumb、NO_COLOR、缺失及损坏依赖，确认无意外联网或配置初始化。

## 2. 分模式安全安装

- [x] 2.1 在 shell 包装器与 bootstrap 增加 `--with-tui` 参数传递、`--help` 与未知选项处理；参数测试确认帮助／参数错误不创建环境，运行 shellcheck。
- [x] 2.2 分离基础和完整预检，按模式生成安装目标与完成提示；在 `tests/test_bootstrap.py` 验证基础不导入 UI、完整检查资源，完整安装失败不静默降级。
- [x] 2.3 扩展离线安装集成测试为 CLI→TUI→CLI 及原安装升级；验证新环境入口、旧环境保留、配置保留、模式提示，以及 venv／pip／预检／发布失败时的入口和 symlink 恢复。

## 3. 石墨黑主题与响应式骨架

- [x] 3.1 在 `manager.tcss` 与界面主题定义中集中语义色，统一细单线边框、文字、焦点和中性按钮；输出实际前景／背景对比度结果，正文 >=4.5:1，必要边框／焦点 >=3:1。
- [x] 3.2 重排标题、导航、正文、常驻两行页脚；通过 Pilot 的 80×24／120×36、往返缩放和滚动测试，验证 >=100 列双栏、窄屏上下布局、焦点滚入与状态栏可达。
- [x] 3.3 重做代理列表与详情区，采用约 65%／35% 比例和独立焦点／活动标记；验证鼠标、方向键、Tab、Enter、全部代理操作按钮可达，移动焦点不修改活动配置。

## 4. 四页细节与弹窗

- [x] 4.1 将诊断改为标签化结果行和解释区，维护按用途分组；测试尚未检查、过期结果、全部维护操作、快照选择／手工路径及迁移默认值，确认未改变服务调用和安全边界。
- [x] 4.2 将命令手册改为宽屏筛选表格＋详情、窄屏上下布局；验证中英文 13 项命令、筛选、长示例完整显示及语言切换。
- [x] 4.3 统一新增／编辑／恢复／迁移表单与确认弹窗，压缩按钮视觉；验证隐藏 URL、校验、取消默认焦点、仅编辑保存、输入框聚焦时不触发单字母操作。
- [x] 4.4 常驻阶段摘要并提供长结果完整查看；复用 fake 操作覆盖执行成功、失败、回滚成功／失败和忙碌门禁，验证重复提交／普通退出被阻止，通知和异常无凭据泄露。

## 5. 双模式回归与视觉验收

- [x] 5.1 更新 CI 为 Python 3.10／3.12 × CLI／TUI 安装模式，调整 UI 测试收集策略和离线 wheels 准备；确认 CLI job 无 Textual／Rich，完整 job 不跳过 UI 测试。
- [x] 5.2 在隔离基础环境运行 CLI／核心测试，在完整环境运行全部 Python 测试；所有安装、操作正确性测试使用临时 XDG 和 fake Codex／app-server，并记录实际结果。
- [x] 5.3 用安全假数据生成 80×24、120×36 的中英文四页及关键弹窗预览，记录截图路径与验收结果；按 designing-tuis 的 P0→P4 顺序逐类修正，检查比例偏差 <=10%、缩放／切页无残影、长内容不丢失、灰度与 NO_COLOR 可用，无 P0/P1 问题才通过。

## 6. 文档与收尾

- [x] 6.1 同步 README.md、README.zh-CN.md 与 AGENTS：默认 CLI、`--with-tui`／`.[tui]`、启动矩阵、完整升级继续带选项、pip 不自动移除 extra、保留旧 venv、`.[test,tui]` 开发方式；核对命令、链接、两种语言和安全示例一致。
- [x] 6.2 运行 `git diff --check`、`python3 scripts/check-secrets.py`、`shellcheck .githooks/pre-commit scripts/install.sh`、`openspec validate make-tui-optional-and-redesign-interface --strict`；复核变更仅涉及本次范围，报告未执行的外部 CI 或真实终端检查，不归档前序变更、不运行生产代理操作。
